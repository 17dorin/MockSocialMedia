@model List<ProfileViewModel>

@if(Model.Count != 0)
{
    foreach(ProfileViewModel p in Model)
    {
        <p>@p.Username</p>
    }
}
else
{
    <h2>You are not currently following any other users</h2>
}
